{
  "name": "gon-landing",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "COMMIT_HASH=$(git rev-parse HEAD 2>/dev/null || echo dev) BRANCH=$(git branch --show-current 2>/dev/null || echo main) BUILD_DATE=$(date -u +%Y-%m-%dT%H:%M:%S) astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^6.0.3",
    "@astrojs/sitemap": "^3.7.3",
    "@astrojs/tailwind": "^6.0.2",
    "@tailwindcss/vite": "^4.3.1",
    "astro": "^6.4.7",
    "tailwindcss": "^4.3.1"
  }
}